function mshadow(selector, distance) {
    const DOM = document.getElementsByClassName
    if (selector > distance) {
        return;
    } else {

    }
};
const g = 'rggrgre';
console.log(g);

const h2DOM = document.getElementById("joe");
console.log(h2DOM);
let h2Text = h2DOM.innerText;
console.log(h2Text);
h2DOM.innerText = "kaiokentimesthree";
console.log(h2DOM);
const ulDOM = document.getElementsByTagName("ul"[0]);
console.log(ulDOM);

const leeDOM = document.querySelectorAll('ul > li');
console.log(leeDOM);
h2DOM.textContent = 'kayyoken';